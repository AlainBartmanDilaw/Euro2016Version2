@using Euro2016.Models;
@model Standings
@{
    ViewBag.Title = "Standings";
}

<h2>Classements</h2>

@using (Html.BeginForm("Index", "Standings", FormMethod.Post))
{
    <fieldset>
@*           @Html.DropDownList("PoolList")
           <input type="submit" value="Valider" />*@
    </fieldset>

    <table>
        @for (int i = 0; i < Model.FullUserStandings.Count; i++)
        {
        <tr>
            <td>@Html.DisplayFor(model => model.FullUserStandings[i].User_Name)</td>
            <td>@Html.DisplayFor(model => model.FullUserStandings[i].PointsTotal)</td>
        </tr>
        }
    </table>
}

@*
@using (@Html.BeginForm("Index", "Standings"))
{

<!DOCTYPE html>
<html>
<body>

<form action="Standings/PoolChoosen">
    <select name="Pool">
        <option value="-1">Tous</option>
        @for(int i=0;i<Model.Pools.Count;i++)
        {
            <option value="">@Html.DisplayFor(model => model.Pools[i].Cod)</option>
        }
    </select>
    <p><input type="button" value="Sélect."</p>
    <table>
    @for (int i = 0; i < Model.FullUserStandings.Count; i++)
    {
    }
    </table>
  <br><br>

</form>

</body>
</html>

}

@section Scripts
{
    @Scripts.Render("~/bundles/jqueryval")
}
*@
